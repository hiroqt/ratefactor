# Neon and Cloudinary audit

Completed locally: Neon runtime baseline, Cloudinary portfolio-cover storage, and two legacy base64 portfolio-cover backfills. All portfolio covers now use HTTPS external or Cloudinary URLs.

GitHub remains the source of truth for transient profile, repository, contribution, and README data. RateFactor keeps GitHub OAuth linkage and durable per-portfolio verification only. Supabase has not been decommissioned; production cutover is pending.
