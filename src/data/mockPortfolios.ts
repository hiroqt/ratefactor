import { Portfolio } from "../types/portfolio";

/**
 * Initial portfolios catalog.
 * Purged of all mock/dummy entries, fake authors, fake ratings, and fake comments.
 * Populated dynamically only by real users via the RateFactor platform and database.
 */
export const INITIAL_PORTFOLIOS: Portfolio[] = [];
